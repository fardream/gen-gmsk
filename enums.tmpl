// Automatically generated by github.com/fardream/gen-gmsk
// enum for {{.CName}}/{{.GoName}}

package {{.PkgName}}

{{if not .IsEqualType }}import "strconv"{{end}}

// {{.GoName}} is {{.CName}}.
{{if .SplitComments}}//
{{end}}{{range .SplitComments}}// {{.}}
{{end -}}
type {{.GoName}} {{if .IsEqualType}}= {{end}} {{.IntegerType}}

const (
{{range .ConstantValues}}{{.}}
{{end -}}
)
{{if not .IsEqualType -}}
var _{{.GoName}}_map = map[{{.GoName}}]string {
{{range .ConstantMaps}}{{.}}
{{end}}
}

func (e {{.GoName}}) String() string {
	if v, ok := _{{.GoName}}_map[e]; ok {
		return v
    }
    return "{{.GoName}}(" + strconv.FormatInt(int64(e), 10) + ")"
}
{{- end}}
